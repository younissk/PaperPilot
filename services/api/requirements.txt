# API Lambda dependencies (MINIMAL)
# The API only handles HTTP requests, DynamoDB, and SQS
#
# NOTE: boto3 is NOT listed here because Lambda runtime provides it.
# This keeps the deployment package small (~100MB vs ~250MB+).

# ASGI adapter for Lambda
mangum>=0.17.0

# Web framework
fastapi>=0.115.0
pydantic>=2.12.5
