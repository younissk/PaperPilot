# Worker Lambda dependencies for Search → Rank → Report pipeline
# NOTE: Plotly and scikit-learn are intentionally excluded (no clustering/viz for now)

# AWS SDK for DynamoDB and S3
boto3>=1.35.0

# Data validation
pydantic>=2.12.5

# Logging
structlog>=24.0.0

# HTTP client for arXiv/OpenAlex API calls
aiohttp>=3.9.0

# OpenAI for LLM-based filtering, ranking, and report generation
openai>=1.12.0

# Progress bars (used by some core modules)
tqdm>=4.67.1
